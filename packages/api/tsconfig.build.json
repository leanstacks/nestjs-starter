/**
 * TypeScript build configuration for the API package.
 */
{
  /* Extend the TypeScript configuration for the API package */
  "extends": "./tsconfig.json",

  /* Compiler options for the API build */
  "compilerOptions": {
    /* Output */
    "rootDir": "./src"
  },
  "include": ["src"],
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}
